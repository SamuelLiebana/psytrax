# Experiment Manifests

Track approval packets and result manifests here. Keep each manifest small and
reviewable. Large logs and binary outputs should be referenced by path rather
than committed.

Use the templates in `.codex/prompts/` for approval packets and Slurm harvest
summaries.
